# `@screeem/forms-react`

TanStack Form bindings for plain `@screeem/forms` definitions.

`useRespondentForm` owns respondent state and submission normalization without
choosing field components or styling. Render `form.Field` with your design
system, or use Screeem's application component as a working reference.
